<%= form_for(@like) do |f| %>
  <% if @like.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@like.errors.count, "error") %> prohibited this like from being saved:</h2>

      <ul>
      <% @like.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= collection_select(:like, :user_id , User.all, :id, :name, {:prompt=>true}) %>
  </div>
  <div class="field">
    <%= f.label :activity_id %><br>
    <%= collection_select(:like, :activity_id, Activity.all, :id, :name, {:prompt=>true}) %>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
